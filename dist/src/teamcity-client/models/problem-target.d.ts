import type { Problems } from './problems';
import type { Tests } from './tests';
export interface ProblemTarget {
    anyProblem?: boolean;
    tests?: Tests;
    problems?: Problems;
}
//# sourceMappingURL=problem-target.d.ts.map