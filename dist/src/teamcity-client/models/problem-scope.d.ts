import type { BuildType } from './build-type';
import type { BuildTypes } from './build-types';
import type { Project } from './project';
export interface ProblemScope {
    project?: Project;
    buildTypes?: BuildTypes;
    buildType?: BuildType;
}
//# sourceMappingURL=problem-scope.d.ts.map